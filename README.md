# Medallion Lakehouse Architecture

This project demonstrates the Medallion Architecture (Bronze, Silver, Gold) using Databricks and Delta Lake. The notebook processes data through multiple layers, applying transformations and aggregations to build a robust data pipeline.

